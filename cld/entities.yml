version: 1

kotlin: import kotlin.js.JsExport

SrvContext:
    prefix-kotlin: @JsExport
    type: context
    fields:
        arguments: [String]
        didLaunch: Bool
        defaultHTTPPort: Int
        httpPort: Int
        inputDir: String
        inputHTTPPort: Int
